# multiple_file_upload_multer

## Install npm dependency
- Run `npm i` command for install dependencies.

## How to run
- Start server with `node index.js` command
- Open http://localhost:3000/ in browser to view UI.

